# MintTrace 开发约定

- 不要过度设计，不要过度思考，不要做过多防御性设计。先完成能演示、能验证的最小闭环。
- 一套 React 界面、一个 Node 服务、一个本地 JSON 数据文件就够了；没有实际需求不要加服务、队列、数据库或抽象层。
- MintTrace 是普通用户的 Web3 来源核验 Agent，包含陌生 NFT、NFT 购买前查出处、活动消息三个入口。只让用户交原始消息、资产链接或公开钱包地址；正确合约和官网由系统调查，不能要求用户先给答案。钱包仅查公开资产，不做 TxLens 的交易模拟、授权分析或自动执行。
- Agent 必须实际调用模型选择工具、跟进证据和生成引用结论。展示的是行动说明与工具结果，不展示私有推理过程。
- Orbio 承担正式版本的 Agent 推理。开发时使用可替换的兼容接口；未用真实 Orbio 密钥验证前，不得声称已经完成 Orbio 实测。
- 密钥只放在被忽略的本地 .env.local，不写前端、不写日志、不写提交。基础输入校验和公开 URL 检查是必要边界，不扩成安全框架。
- 只初始化 Codex 的 UI/UX Pro Max skill。前端遵循 design-system.md：独立产品首页与简洁核验工作台，保持键盘可用、手机可用、错误可见。
- 示例必须明确标为合成数据。Agent 的示例调查也必须走真实模型；内置示例报告不得伪装为实时调查。
- 官方身份来自独立核对的目录及实际读取的官方资料；可用已读取官网明确指向的社交账号交叉核对对应帖子。消息内容和来源身份分开判断：页面有原文但身份未核实时可以给出 source_supported，不能把已读取的信息说成完全未知。待查页面自称官方不是身份依据；未收录或黑名单未命中不能说安全。
- 缺少证据就说无法确认。地址不一致不等于诈骗，名称或图片相似不等于归属；不提供买卖结论。
- 变更后运行 npm test、npm run build；改变界面时检查实际浏览器。测试关键行为，不堆与实现重复的测试。
- 默认不自动消耗额度。复查由用户点击触发；不要偷偷启动定时任务。

## Commands

`npm install` · `npm run dev` · `npm test` · `npm run build` · `npm start`

- 界面默认跟随浏览器中英文语言，可手动覆盖；新核验的报告和行动说明使用所选语言。保留历史报告及引用原文，不因切换语言自动消耗模型额度。
- 跟进来源保留已读取页面中的全部已提取链接，不套用用户输入的 8 链接上限；记录失败网址及原因，区分程序未发出请求与网站/网络读取失败。

- `/` 是产品展示首页，`/app` 是核验应用；旧报告链接要兼容。首页动画与报告示意必须明确标识，不得产生模型调用。
